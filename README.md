# MAS 2xxx notes

## Hosting Locally

- Within the MAS2xxx file directory type `jupyter-book build .`
- Type `cd _build/html`
- Type `python -m http.server`
- Go to `http://localhost:8000`

To remake the notebook type `jupyter-book clean .` followed by a rebuild in the correct directory.